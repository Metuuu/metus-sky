using UnityEngine;
using System.Collections;


public class SphericalCubeTest : MonoBehaviour {

    public string name;
    public int size;
    public int gridSize;

    // TODO: poista tää vanhentunut scripti

    // Use this for initialization
    void Start () {
        
        /*
        GameObject objj = new GameObject("xxx");
        Mesh meshi = SphericalCubeGenerator.GenerateMesh(name, size, gridSize, "1");
        objj.AddComponent<MeshFilter>().mesh = meshi;
        objj.AddComponent<MeshRenderer>();
        */
    }

    // Update is called once per frame
    void Update () {
    
    }
}